# Customer-layer files — NEVER committed.
# This repo only holds the E.V.E. *platform* layer (vendor).
# Customer-specific content lives on the customer's own box, in their .gitignored area.

# Composed file (auto-generated by assemble-claude.sh on each box)
CLAUDE.md

# Customer-layer counterparts
CLAUDE.user.md
memory/user/

# Vault content (customer-specific knowledge)
00-Inbox/
01-Daily/
02-Projects/
03-People/
05-Archive/

# Credentials and secrets
.config/eve/
.google_workspace_mcp/
*.env
*.env.bak*
backup-passphrase
*.key
*.pem
credentials.json
*-credentials.json
*token.json

# WhatsApp bridge session state
whatsapp-bridge/store/
wake-config.json

# Cloudflared tunnel credentials
.cloudflared/

# Generated artifacts
*-cache/
*-state/
vault-chroma/
eve-knowledge.db
*.log
__pycache__/
*.pyc

# Python venvs (recreated by install.sh)
*-venv/
.venv/
node_modules/

# Compiled artifacts
whatsapp-bridge/whatsapp-bridge
*.so
*.o

# OS junk
.DS_Store
*.swp
*~
